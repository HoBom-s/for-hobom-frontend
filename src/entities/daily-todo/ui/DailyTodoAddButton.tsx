import { IconButton } from "@mui/material";
import { AddCircle } from "@mui/icons-material";

export const DailyTodoAddButton = () => {
  return (
    <IconButton size="small">
      <AddCircle fontSize="small" />
    </IconButton>
  );
};
