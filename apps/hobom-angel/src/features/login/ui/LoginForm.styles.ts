import * as stylex from "@stylexjs/stylex";

const DESKTOP = "@media (min-width: 768px)";

export const styles = stylex.create({
  page: {
    minHeight: "100vh",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "clamp(16px, 4vw, 40px)",
    backgroundColor: "var(--hb-angel-surface-alt)",
  },
  card: {
    width: "100%",
    maxWidth: 960,
    display: "grid",
    gridTemplateColumns: { default: "1fr", [DESKTOP]: "1fr 1fr" },
    backgroundColor: "var(--hb-color-surface)",
    borderRadius: 24,
    overflow: "hidden",
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "var(--hb-color-border)",
    boxShadow: "var(--hb-angel-shadow)",
  },

  // ── Brand panel (desktop only) ──────────────────────────
  brand: {
    display: { default: "none", [DESKTOP]: "flex" },
    position: "relative",
    overflow: "hidden",
    flexDirection: "column",
    justifyContent: "space-between",
    gap: 24,
    padding: 44,
    backgroundImage:
      "linear-gradient(160deg, var(--hb-color-accent) 0%, var(--hb-angel-green-deep) 100%)",
    color: "#ffffff",
  },
  blob: {
    position: "absolute",
    top: -60,
    right: -40,
    width: 220,
    height: 220,
    borderRadius: "50%",
    backgroundColor: "rgba(255,255,255,0.12)",
  },
  brandLogo: {
    position: "relative",
    display: "inline-flex",
    alignItems: "center",
    gap: 8,
    fontWeight: 700,
    fontSize: "1.0625rem",
  },
  brandLogoIcon: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    width: 28,
    height: 28,
    borderRadius: 9,
    backgroundColor: "rgba(255,255,255,0.2)",
    fontSize: "0.9rem",
  },
  brandTitle: {
    position: "relative",
    margin: 0,
    fontSize: "1.75rem",
    fontWeight: 800,
    lineHeight: 1.3,
    letterSpacing: "-0.02em",
  },
  brandLead: {
    position: "relative",
    margin: 0,
    marginTop: 12,
    fontSize: "0.9375rem",
    lineHeight: 1.6,
    color: "rgba(255,255,255,0.85)",
  },
  brandStat: { position: "relative", fontSize: "0.8125rem", color: "rgba(255,255,255,0.75)" },

  // ── Form panel ──────────────────────────────────────────
  form: {
    paddingBlock: { default: 32, [DESKTOP]: 44 },
    paddingInline: { default: 24, [DESKTOP]: 44 },
    display: "flex",
    flexDirection: "column",
  },
  title: { margin: 0, fontSize: "1.5rem", fontWeight: 800, color: "var(--hb-color-text-primary)" },
  subtitle: {
    margin: 0,
    marginTop: 6,
    marginBottom: 24,
    fontSize: "0.9375rem",
    color: "var(--hb-color-text-secondary)",
  },
  fields: { display: "flex", flexDirection: "column", gap: 16 },
  pwLabelRow: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    marginBottom: 6,
  },
  labelText: { fontSize: "0.875rem", fontWeight: 600, color: "var(--hb-color-text-primary)" },
  link: { fontSize: "0.8125rem", fontWeight: 600, color: "var(--hb-color-accent-dark)" },
  toggle: {
    borderWidth: 0,
    borderStyle: "none",
    backgroundColor: "transparent",
    cursor: "pointer",
    fontSize: "0.8125rem",
    color: "var(--hb-color-text-secondary)",
    paddingInline: 8,
  },
  remember: {
    marginTop: 16,
    display: "flex",
    alignItems: "center",
    gap: 8,
    fontSize: "0.875rem",
    color: "var(--hb-color-text-secondary)",
    cursor: "pointer",
  },
  formError: {
    margin: 0,
    marginTop: 16,
    fontSize: "0.875rem",
    color: "var(--hb-color-danger, #d92d20)",
  },
  submit: { marginTop: 20 },
  divider: {
    display: "flex",
    alignItems: "center",
    gap: 12,
    marginBlock: 20,
    color: "var(--hb-color-text-secondary)",
    fontSize: "0.8125rem",
  },
  dividerLine: { flex: 1, height: 1, backgroundColor: "var(--hb-color-border)" },
  signup: { textAlign: "center", fontSize: "0.875rem", color: "var(--hb-color-text-secondary)" },
  signupLink: { fontWeight: 700, color: "var(--hb-color-accent-dark)" },
});
